#!/usr/bin/env pyt

# pip install scapy
import scapy.all as scapy

def scan(ip):
    scapy.arping(ip)

scan("192.168.1.1")  <== find only client with given IP
scan("192.168.1.1/24")  <== find each client with IP from range